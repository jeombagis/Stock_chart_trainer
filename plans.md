# S&P 500 · NASDAQ-100 차트 트레이너 구현 계획

## 1. 목표와 사용자 흐름

사용자는 S&P 500 또는 NASDAQ-100을 선택하고, 과거 일봉 캔들의 일부 구간만 본 뒤 다음 구간의 방향을 `상승`, `횡보`, `하락` 중 하나로 예측한다. 답안을 제출하면 숨겨 둔 후속 일봉 차트와 실제 결과를 표시하고, 예측의 정오답 및 누적 성적을 확인한다.

기본 흐름은 다음과 같다.

1. 지수와 난이도(표시 기간, 예측 기간)를 선택한다.
2. 서버가 무작위 기준일을 고르고, 기준일 이전의 캔들만 클라이언트에 보낸다.
3. 사용자가 다음 구간의 방향을 선택해 제출한다.
4. 서버가 실제 후속 가격으로 라벨을 판정해 반환한다.
5. 클라이언트가 후속 캔들을 이어 그리며 정답, 수익률, 누적 통계를 보여 준다.

## 2. 범위와 핵심 정책

- 데이터 제공처: Yahoo Finance.
- 대상 심볼: S&P 500 `^GSPC`, NASDAQ-100 `^NDX`.
- 봉 단위: 거래일 기준 1일(OHLCV) 캔들.
- 기본 설정: 과거 60거래일을 노출하고, 뒤 20거래일을 예측 대상으로 사용한다.
- 방향 판정: 기준일 종가 대비 예측 종료일 종가의 수익률로 결정한다.
  - 상승: 수익률 `> +2%`
  - 하락: 수익률 `< -2%`
  - 횡보: `-2% ~ +2%`
- 임계값·표시일·예측일은 설정값으로 분리해 나중에 난이도별로 조절한다.
- 미래 데이터는 퀴즈 시작 시 절대로 브라우저에 내려 보내지 않는다. 정답을 맞힌 뒤에만 후속 OHLC 데이터를 제공한다.

## 3. 권장 기술 구성

- 프런트엔드: React + TypeScript + Vite.
- 차트: `lightweight-charts`(캔들, 시간축, 후속 구간 강조 표시 지원).
- 백엔드: Node.js + TypeScript + Express 또는 Fastify.
- Yahoo Finance 연동: 서버에서 `yahoo-finance2`를 사용하거나 Yahoo chart endpoint를 감싼 `MarketDataProvider` 인터페이스를 구현한다.
- 저장소: 초기에는 SQLite(퀴즈 결과와 설정), 이후 필요하면 PostgreSQL로 전환한다.
- 테스트: Vitest(도메인/프런트 단위 테스트), Supertest(HTTP API), Playwright(핵심 사용자 흐름).

## 4. 데이터 수집 및 정규화

1. `MarketDataProvider.getDailyHistory(symbol, from, to)` 인터페이스를 만든다.
2. Yahoo Finance에서 `^GSPC`, `^NDX`의 일봉 `open/high/low/close/volume/date`를 조회한다.
3. 결측·중복 날짜·비정상 OHLC 값을 제거하고 날짜순으로 정렬한다.
4. 장 마감 데이터 갱신 지연 및 호출 제한에 대비해 원본 응답과 정규화 결과를 캐시한다.
   - 최근 데이터: 짧은 TTL 캐시.
   - 완료된 과거 구간: DB 또는 파일 캐시로 장기 보관.
5. 수집 실패 시 마지막 정상 캐시를 사용하고, 화면에는 데이터 기준일과 오류 상태를 명시한다.
6. 개발·테스트 환경에는 고정된 fixture 데이터를 둬서 Yahoo 응답 변화와 무관하게 테스트한다.

## 5. 퀴즈 도메인 설계

### 주요 모델

- `Instrument`: `id`, `symbol`, `name` (`sp500`, `nasdaq100`).
- `Candle`: `date`, `open`, `high`, `low`, `close`, `volume`.
- `QuizConfig`: `instrument`, `visibleDays`, `forecastDays`, `sidewaysThreshold`.
- `Quiz`: `id`, `instrument`, `cutoffDate`, `visibleCandles`, `forecastEndDate`, `status`.
- `QuizAnswer`: `quizId`, `prediction`, `actualLabel`, `returnPercent`, `isCorrect`.
- `QuizResult`: `quizId`, `revealedCandles`, `answer`, `scoreSummary`.

### 출제 알고리즘

1. 종목별로 충분한 거래일을 가진 정규화 데이터를 준비한다.
2. `visibleDays + forecastDays`를 모두 만족하는 기준일 후보만 만든다.
3. 최근 데이터만 반복되지 않도록 후보 범위와 최근 출제 이력을 고려해 무작위 선택한다.
4. 기준일 이전 `visibleDays`만 `POST /api/quizzes` 응답에 포함한다.
5. 답안 제출 시 서버가 보관한 기준일과 후속 종가로 실제 라벨·수익률을 계산한다.
6. `revealedCandles`는 `POST /api/quizzes/:id/answer` 응답에서 처음 반환한다.

## 6. API 설계

| API | 역할 |
| --- | --- |
| `GET /api/instruments` | 지원 지수와 최신 데이터 기준일 반환 |
| `POST /api/quizzes` | 설정값을 받고 새 문제와 노출 캔들만 반환 |
| `POST /api/quizzes/:id/answer` | 예측값을 받아 정답, 수익률, 후속 캔들 반환 |
| `GET /api/stats` | 전체·지수별·방향별 정답률 반환 |
| `POST /api/data/refresh` | 관리자/개발용 데이터 갱신 실행 |

`POST /api/quizzes`와 답안 API에는 Zod 등의 런타임 검증을 적용한다. 서버는 만료·재제출·존재하지 않는 퀴즈를 명확한 오류 코드로 처리한다.

## 7. 화면 구성

- 시작 화면: 지수 선택, 표시 기간/예측 기간, 시작 버튼.
- 퀴즈 화면: 제목·기준일·캔들 차트·예측 버튼 세 개·제출 버튼.
- 제출 전: 기준일 이후 데이터가 차트 범위에 포함되지 않도록 x축 범위와 자동 스케일을 제한한다.
- 결과 화면: 후속 캔들을 다른 색 또는 음영 영역으로 확장하고, 실제 방향·수익률·정오답·다음 문제 버튼을 표시한다.
- 통계 화면/패널: 총 문제 수, 정답률, 지수별 정답률, 상승/횡보/하락별 혼동 행렬을 표시한다.
- 접근성: 버튼 키보드 조작, 선택 상태의 텍스트 표시, 색상만으로 상승/하락을 구별하지 않는 보조 표기를 제공한다.

## 8. 단계별 개발 순서

### 1단계: 기반 구성

- React/TypeScript 앱과 API 서버를 구성한다.
- 환경 변수, 린트·포맷터·테스트 실행 명령을 추가한다.
- `MarketDataProvider`와 fixture 기반 구현을 먼저 만든다.

### 2단계: 시장 데이터 계층

- Yahoo Finance 어댑터, 데이터 정규화, 캐시, 갱신 작업을 구현한다.
- 두 심볼의 일봉을 내려받아 품질 검사를 수행한다.
- 실패·빈 데이터·휴장일 케이스를 테스트한다.

### 3단계: 퀴즈 엔진과 API

- 문제 생성, 미래 데이터 차단, 방향 판정, 답안 처리 로직을 구현한다.
- SQLite에 문제·답안·통계를 저장한다.
- 경계값(+2%, -2%), 기준일 후보 부족, 중복 제출을 단위/API 테스트로 검증한다.

### 4단계: 차트와 사용자 경험

- 노출 구간 캔들 차트를 구현한다.
- 예측 선택·제출·결과 공개 상태를 연결한다.
- 결과에서 노출/후속 구간을 시각적으로 명확히 구분하고 통계를 갱신한다.

### 5단계: 품질 보강 및 배포 준비

- 로딩·오류·캐시 기준일 상태를 추가한다.
- 모바일 화면과 키보드 사용성을 점검한다.
- Playwright로 “문제 시작 → 예측 → 정답 공개 → 다음 문제” 흐름을 자동 검증한다.
- Yahoo Finance의 비공식 데이터 접근 특성과 사용 조건을 README에 기록하고, 공급자 교체가 가능한 구조를 유지한다.

## 9. 완료 기준

- S&P 500과 NASDAQ-100 모두에서 과거 일봉 문제를 생성할 수 있다.
- 답안을 내기 전 네트워크 응답과 화면에 후속 OHLC 정보가 없다.
- 설정한 임계값에 따라 상승·횡보·하락 판정이 일관되게 이루어진다.
- 정답 제출 뒤 정확한 후속 캔들과 수익률·정오답이 표시된다.
- 핵심 도메인 로직과 API, 브라우저 사용자 흐름 테스트가 통과한다.
